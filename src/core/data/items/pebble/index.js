/**
 * Item de teste genérico (throwable) — valida o mecanismo de item (registro,
 * `HeldItem`, seletor de debug), não é conteúdo de jogo de verdade.
 */
export const PEBBLE = {
  id: 'pebble',
  category: 'throwable',
}
